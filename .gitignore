/target
*.identity
*.lock
!Cargo.lock

# Launcher download artifacts (runtime, not source): the steamcmd bootstrap
# cache, a pulled Sven Co-op dedicated server, and the remembered install path.
/steamcmd/
/svends/
/.svends_path

# GUI (Tauri) build artifacts.
/gui/src-tauri/target/
/gui/src-tauri/gen/

# Controller crate build artifacts.
/controller/target/

# Release artifacts (built locally; distributed via GitHub Releases, not git).
/release/