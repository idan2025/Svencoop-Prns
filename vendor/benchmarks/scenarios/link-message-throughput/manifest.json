{
  "name": "link-message-throughput",
  "version": 8,
  "order": 2,
  "title": "Link-message throughput",
  "category": "Links",
  "summary": "Sustained delivery of small messages over one established link.",
  "primary_metric": "delivered_per_sec",
  "headline": true,
  "notes": [],
  "description": "One link saturated with windowed small single-packet sends for a fixed wall-time - the same data shape as single-packet-throughput carried by the other mechanism: a session key amortizes the per-message crypto that singles pay per packet, while ProveAll still proves every delivery. The reference participant wakes on receipt callbacks and immediately checks the armed receipt's status, covering proofs that beat callback registration without adding a polling delay. The contrast between the two firehoses is the measurement.",
  "roles": [
    "responder",
    "initiator"
  ],
  "profile": {
    "mechanism": "link",
    "window": 16,
    "duration_ms": 30000,
    "drain_timeout_ms": 30000,
    "payload_min": 60,
    "payload_max": 420,
    "size_seed": 6840346605343539201
  },
  "conformance_rule": "exact-link",
  "conformance": "attempted == sent + locally_culled, sent == responder_delivered, payload bytes match exactly, and wire timeouts are zero. Final reference receipts that lose their proof only after traffic stops remain explicit as receipt_unproved; they count as delivered only when the responder independently confirms the exact message and byte totals."
}
