{
  "name": "single-packet-throughput",
  "version": 6,
  "order": 1,
  "title": "Single-packet throughput",
  "category": "Packets",
  "summary": "Sustained proved delivery of varied-size one-shot packets over TCP.",
  "primary_metric": "delivered_per_sec",
  "headline": true,
  "notes": [],
  "description": "A ProveAll destination saturated with windowed SINGLE packets for a fixed wall-time: sustained one-shot message throughput, goodput, and settlement latency from the protocol's own proofs. No link - singles are the protocol's native shape for high-volume small one-shots, each proof carrying the RTT. The reference participant wakes on receipt callbacks and immediately checks the armed receipt's status, covering proofs that beat callback registration without adding a polling delay.",
  "roles": [
    "responder",
    "initiator"
  ],
  "profile": {
    "mechanism": "single",
    "window": 16,
    "duration_ms": 30000,
    "drain_timeout_ms": 30000,
    "payload_min": 60,
    "payload_max": 380,
    "size_seed": 6840346605343539201
  },
  "conformance_rule": "exact-single",
  "conformance": "Every sent packet is proved and independently seen by the responder: sent == delivered == responder_delivered, with zero timeouts, races, or local culls."
}
