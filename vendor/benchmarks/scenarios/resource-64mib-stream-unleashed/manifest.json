{
  "name": "resource-64mib-stream-unleashed",
  "version": 3,
  "order": 7,
  "title": "64-segment resource stream · 1 Gbps policy",
  "category": "Resources",
  "summary": "Stream 64 maximum-efficient resource segments with both endpoint TCP interfaces explicitly configured for the 1 Gbps MTU tier.",
  "primary_metric": "goodput_bytes_per_sec",
  "headline": true,
  "notes": [
    "Controlled computational comparison: identical workload and protocol, with only TCP bitrate policy changed.",
    "Both implementations carry the same 67,108,800-byte payload in 64 maximum-efficient protocol segments. This is 64 bytes below 64 MiB and avoids making benchmark completion depend on RNS {reference_version}'s timing-sensitive handoff to a 65th 64-byte tail segment."
  ],
  "cell_notes": [
    {
      "subject": {
        "path": "direct",
        "initiator": "rns-1.4.0-compiled",
        "responder": "personal-rns"
      },
      "text": "RNS prepares the next 1 MiB segment in a background thread. Prns proves the current segment before that preparation completes, making RNS enter a coarse 50 ms polling loop. A slower stock receiver gives preparation enough time to finish, avoiding that cliff."
    },
    {
      "subject": {
        "path": "direct",
        "initiator": "rns-1.4.2-compiled",
        "responder": "personal-rns"
      },
      "text": "RNS prepares the next 1 MiB segment in a background thread. Prns proves the current segment before that preparation completes, making RNS enter a coarse 50 ms polling loop. A slower stock receiver gives preparation enough time to finish, avoiding that cliff."
    }
  ],
  "description": "The 64-segment incompressible resource stream repeated with both real initiator and responder TCP interfaces explicitly configured for 1 Gbps. That policy selects the 524,288-byte interface MTU tier on both stacks. Compression remains disabled and every segment remains encrypted, hash-proved, assembled, and application-acknowledged; this is the controlled counterpart to the default-policy result.",
  "roles": [
    "responder",
    "initiator"
  ],
  "profile": {
    "mechanism": "resource",
    "payload_len": 67108800,
    "window": 1,
    "tcp_bitrate_bps": 1000000000,
    "duration_ms": 30000,
    "drain_timeout_ms": 30000,
    "compression": "off",
    "size_seed": 6840346605343539201
  },
  "conformance_rule": "exact-resource",
  "conformance": "Every sent stream settles, receives its ordered application acknowledgement, and is received with exact application-byte totals and zero failures; each transfer is a sequence of hash-proved segments assembled by original_hash before the whole is counted."
}
